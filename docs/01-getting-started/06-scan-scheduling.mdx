---
title: Schedule scans
---

# ⏰ Schedule scan

By default, the scan is scheduled to run every day at the time you created the app. You can change the schedule by clicking on the `Schedule` tab in the `CI/CD` section.

CRON is supported.

<Image img={require('./img/schedule-scan.png')} alt="test"/>
